# inventory.yml
all:
  children:
    server:
      hosts:
        ec2-server:
          ansible_host: "{{ (lookup('file', '../../bootstrap_terraform_output.json') | from_json).jenkins_ip.value }}"
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ../ci-ssh-key.pem